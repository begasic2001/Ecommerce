// Link component
export interface ILinkItem {
  title: string;
  list: string[];
}

// ---------------------------------------
// Styles CSS
export interface ISFooter {
  footer: string;
  container: string;
}

export interface ISLink {
  link: string;
  linkItem: string;
  linkTitle: string;
  linkList: string;
}

export interface ISSocial {
  social: string;
  socialPaypal: string;
  socialTxt: string;
}
